// 1. write a function min(a,b) which returns the least of two no. a and b.

function min(a,b){
    if(a<b){
        return a;
    }
    else{
        return b;
    }
    }
    result=min(10,17);
    console.log(result)