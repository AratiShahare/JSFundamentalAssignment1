/* 1. here's a code that asks the user for two no. and showsz their sum. 
      it works incorrectly. the output in the example below is 12 
      (for default promt values.)
      why? fix it. the result should be 3.
*/
  let a= prompt("first number?",1);
  let b= prompt("first number?",2);
  a=parseInt(a)
  b=parseInt(b)
  alert(a+b);
